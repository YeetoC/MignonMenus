# Bookmarks (shadcn/ui + Radix UI)

This repo contains the **Bookmarks** template (Next.js + shadcn/ui + Tailwind).

## Supabase

Database schema migrations live in `supabase/migrations/`.
See `supabase/README.md` for the local workflow.

## Dev

```bash
pnpm install
pnpm dev
```
